export const dummy = (): void => {};

// const weaponRange = data.shortRangeBand
// ? {
//     short: data.shortRangeBand,
//     medium: data.mediumRangeBand,
//     long: data.longRangeBand,
//     maximum: data.maximumRangeBand,
//   }
// : undefined;

// const weaponMode: WeaponModeType = {
// name: data.name,
// damage: data.damage,
// burst: data.burst,
// ammo: data.ammo,
// combinedAmmo: data.combinedAmmo,
// traits: data.traits,
// weaponRange,
// };

// setWeapon(weapon => {
// if (!weapon?.weaponModes) {
//   return { ...weapon, weaponModes: [weaponMode] };
// }

// return { ...weapon, weaponModes: [...weapon.weaponModes, weaponMode] };
// });
